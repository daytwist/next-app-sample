# Copy from .env.local on the Vercel dashboard
# https://nextjs.org/learn/dashboard-app/setting-up-your-database#create-a-postgres-database
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Supabase (DB は Next.js サーバーからのみ利用)
# クライアントへは公開しないため、NEXT_PUBLIC_* は設定しないこと。
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Optional: Supabase JWT secret (RLS 関連で使用する場合)
SUPABASE_JWT_SECRET=

# `openssl rand -base64 32`
AUTH_SECRET=
AUTH_URL=http://localhost:3000/api/auth
